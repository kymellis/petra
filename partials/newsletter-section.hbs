{{#unless @member}}
  {{#if @site.members_enabled}}
    {{#unless @site.members_invite_only}}
      <div class='c-section c-section--newsletter'>
        <div class='o-grid'>
          <div class='c-content u-align-center'>
            <p class='u-font-heading'>{{t 'Subscribe to receive the latest posts in your inbox.' }}</p>
            {{> newsletter-form inputLabel='global-newsletter-form'}}
             <p class='u-font-heading'>{{t '*I have read and agree to the Plant Magic Association Membership Agreement 🔗' }}</p>
          </div>
        </div>
      </div>
    {{/unless}}
  {{/if}}
{{/unless}}